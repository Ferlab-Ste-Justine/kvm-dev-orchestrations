apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: minio-audit-alert-fluentd-config
    options:
      disableNameSuffixHash: false
    files:
      - fluentd.conf