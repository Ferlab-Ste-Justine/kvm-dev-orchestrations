<match vault-tunnel-load-balancer>
  @type file
  path /opt/logs/vault/tunnel/transport-load-balancer

  <format>
    @type single_value
    add_newline true
    message_key compact_version
  </format>
</match>

<match vault-tunnel-control-plane>
  @type file
  path /opt/logs/vault/tunnel/transport-control-plane

  <format>
    @type single_value
    add_newline true
    message_key compact_version
  </format>
</match>

<match vault-tunnel-node-exporter>
  @type file
  path /opt/logs/vault/tunnel/node-exporter

  <format>
    @type single_value
    add_newline true
    message_key compact_version
  </format>
</match>